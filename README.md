# create-next-quickstart

A tool to quickly create a Next.js project with a basic setup.

## Installation

### Using npx (without installation)

If you want to create a new project without installing this package locally, use `npx`:

```bash
npx create-next-quickstart my-new-project
```
